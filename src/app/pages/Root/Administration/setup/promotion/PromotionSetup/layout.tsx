import { Outlet } from 'react-router-dom'

function PromotionSetUpLayout() {
  return (
    <div>
      <h4>Promotion SetUp Layout</h4>
      <main>
        <Outlet />
      </main>
    </div>
  )
}

export default PromotionSetUpLayout
